// Function to make juice using three ingredients
function makeJuice(fruit1, fruit2, liquid) {
  // Combine the three ingredients into one string
  return `Juice made of ${fruit1} + ${fruit2} + ${liquid}`;
}

// Call the function and print the returned value
console.log(makeJuice("Apple", "Banana", "Milk"));
